machine:
  services:
    - docker

dependencies:
  override:
    - docker pull ambermd/manylinux-extra

test:
  override:
    - source setup.sh
  post:
    - mkdir $CIRCLE_ARTIFACTS/amber-build/
    - cp ambertools*bz2 $CIRCLE_ARTIFACTS/amber-build/
