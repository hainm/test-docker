os:
- linux
language: c
sudo: required
services: docker
install:
    - source scripts/install_git_lfs.sh
    - tar -xf A.tar.bz2 && cp -rf recipe-prebuild amber16/
    - source run_docker_build.sh
script: echo "hello"
after_success:
    - docker images
notifications:
  email: false
