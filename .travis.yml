matrix:
  include:
   - { os: osx, env: AMBER_BUILD_TASK=ambermini }
   - { os: linux, env: AMBER_BUILD_TASK=ambertools }
language: c
install:
    # - source build.sh
    - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then
          brew tap homebrew/science;
          brew update;
          brew install gcc;
      fi
    - source scripts/install_miniconda.sh
    - conda build recipe
script: echo "hello"
after_success:
    - echo "hello"
notifications:
  email: false
